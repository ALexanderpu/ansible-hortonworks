sudo apt-get install -y vim git python-pip openssh-server 
# edit /etc/hosts with host0, host1, host2
# ssh-keygen   ssh-copy-id bo@hosti  for i in [0,1,2]

# service start order: hdfs, zookeeper, yarn, mapreduce2, hive, spark2
